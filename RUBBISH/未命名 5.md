设111个单位向量为 $\vec{v}_1, \vec{v}_2, \dots, \vec{v}_{111}$，满足 $\sum_{i=1}^{111} \vec{v}_i = \vec{0}$，不妨按极角从小到大排序.
任取一个55元子集 $S$，记其和为 $\vec{s} = \sum_{i \in S} \vec{v}_i$，则其补集 $S^c$ 的和 $\vec{s}^c = \sum_{i \in S^c} \vec{v}_i = -\vec{s}$.

对于 $k = 1, 2, \ldots, 111$，在模111的意义下定义：
$$\vec{S}_k = \vec{v}_k + \vec{v}_{k+1} + \cdots + \vec{v}_{k+54}$$

对任意 $k$，有 $\vec{S}_{k+55} = -\vec{S}_k - \vec{v}_{k-1}$。
下证：
由 $\sum_{i=1}^{111} \vec{v}_i = \vec{0}$，$\vec{S}_k + \vec{S}_{k+55} + \vec{v}_{k+110} = \vec{0}$.
又 $\vec{v}_{k+110} = \vec{v}_{k-1}$，因此$\vec{S}_{k+55} = -\vec{S}_k - \vec{v}_{k-1}$

两边取模长，$|\vec{S}_{k+55} + \vec{S}_k| = |\vec{v}_{k-1}| = 1$.

假设：对所有 $k = 1, 2, \ldots, 111$，都有 $|\vec{S}_k| > 1$。

设 $|\vec{S}_k| = a > 1$，$|\vec{S}_{k+55}| = b > 1$.
$|\vec{S}_{k+55} + \vec{S}_k|^2 = a^2 + b^2 + 2ab\cos\theta = 1$，其中 $\theta$ 是 $\vec{S}_k$ 和 $\vec{S}_{k+55}$ 的夹角.
由于 $a > 1$，$b > 1$， $a^2 + b^2 > 2$.
因此
$$
1 = a^2 + b^2 + 2ab\cos\theta > 2 + 2ab\cos\theta
\implies \cos\theta < -\frac{1}{2ab} < 0
$$
说明$\frac{3\pi}{2}> \theta > \frac{\pi}{2}$.

注意到，$\vec{S}_{k+1} = \vec{S}_k - \vec{v}_k + \vec{v}_{k+55}$
因此
$$|\vec{S}_{k+1} - \vec{S}_k| = |\vec{v}_{k+55} - \vec{v}_k| \leq |\vec{v}_{k+55}| + |\vec{v}_k| = 2$$
说明相邻两项的距离不超过2.

考虑从 $\vec{S}_k$ 到 $\vec{S}_{k+55}$ 的路径.
由$\vec{S}_{k+55} = -\vec{S}_k - \vec{v}_{k-1}$.
如果 $|\vec{S}_k| > 1$，则 $\vec{S}_k$ 和 $\vec{S}_{k+55}$ 都在单位圆外.
由于 $\vec{S}_k$ 和 $-\vec{S}_k$ 关于原点对称.  $\vec{S}_{k+55}$ 与 $-\vec{S}_k$ 的距离为 $|\vec{v}_{k-1}| = 1$，且路径 $\vec{S}_k \to \vec{S}_{k+1} \to \cdots \to \vec{S}_{k+55}$ 是连续的，由介值定理，该路径必须穿过单位圆.
这意味着路径上存在某个点 $\vec{S_m}$ 满足 $|\vec{S_m}| ≤ 1$.

这与假设矛盾！

